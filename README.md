Testing Uncontrolled and Controlled components with react/

1. Controlled input provides value managment via useState;
2. Uncontrolled Input provides access to the DOM value via useRef;
3. Fetches users from https://jsonplaceholder.typicode.com/users once.

Filter users locally based on the Controlled Input value.

Displays loading and error states.
